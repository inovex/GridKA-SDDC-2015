#!/bin/bash
#On VM's (best over ssh -> you can use the hostnames): 
echo "Master-Priv-IP		kit-mesos-master" >> /etc/hosts
echo "Slave1-Priv-IP		kit-mesos-slave1" >> /etc/hosts
echo "Slave2-Priv-IP		kit-mesos-slave2" >> /etc/hosts
echo "Slave3-Priv-IP		kit-mesos-slave3" >> /etc/hosts
